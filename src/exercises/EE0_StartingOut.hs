{- HLINT ignore "Use camelCase" -}

module EE0_StartingOut where

-- To ensure your setup is working, replace the typed hole below with a boolean value
a_iWantToLearnEsqueleto :: Bool
a_iWantToLearnEsqueleto = _

{-

Check out the "Getting Started" section in the README if you need to.

If you fill the typed hole above with `False`, you can check that the test suite
is catching mistakes.

If you leave the typed hole around, you should see a compilation warning that
turns into an runtime error when the test tries to run.

Once the tests pass, move on to `src/EE1_Select.hs`

-}
